# ============================================================
# 开发环境配置
# 启动时指定 --spring.profiles.active=dev 激活
# ============================================================

# 开发环境可覆盖主配置中的任何属性
spring:
  devtools:
    restart:
      enabled: true
